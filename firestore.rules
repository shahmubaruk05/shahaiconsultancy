rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP DEV RULE: everything open
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
